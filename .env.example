# Local development placeholders only. Never commit real secrets.
POSTGRES_DB=invoicenow_workbench
POSTGRES_USER=invoicenow_app
POSTGRES_PASSWORD=replace_for_local_development
SPRING_PROFILES_ACTIVE=local
FRONTEND_ORIGIN=http://localhost:5173

# Optional AI extension; leave disabled during the deterministic MVP.
AI_ASSISTANT_ENABLED=false
